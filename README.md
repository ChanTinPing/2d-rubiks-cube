# 二维魔方

一个静态网页版 3x3x3 二维魔方游戏。

## 打开方式

直接用浏览器打开 `index.html`，或在项目目录启动任意静态服务器。

```powershell
python -m http.server 4174 --bind 127.0.0.1
```

然后访问 `http://127.0.0.1:4174/`。

## 等价性

游戏状态由 54 个贴纸组成，每个贴纸都有三维位置和法向量。`U/D/L/R/F/B` 面转会对对应三维层执行四分之一旋转，二维棋盘和右上角三维预览都从同一个状态渲染，因此二维局面始终是合法 3x3x3 魔方状态。
